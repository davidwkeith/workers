---
"@dwk/micropub": minor
---

Add the opt-in proposed Location/Venue (`q=geo`) extension: a proximity
search over an injectable D1-backed venue store, accepting a Geo URI or
`lat`/`lon`/`u` coordinates and returning nearby venues ordered by distance
with offset pagination.
