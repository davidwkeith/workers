{
  "name": "dwk-discovery-starter",
  "version": "0.0.0",
  "private": true,
  "description": "One-click Deploy to Cloudflare starter: @dwk/webfinger + @dwk/host-meta composed into a single zero-binding Worker.",
  "type": "module",
  "license": "ISC",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@dwk/host-meta": "0.1.0-beta.4",
    "@dwk/webfinger": "0.1.0-beta.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260718.1",
    "typescript": "6.0.3",
    "wrangler": "4.112.0"
  }
}
